apiVersion: v1
kind: Service
metadata:
    name: assignmentpartazurekubernetesrepo
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: assignmentpartazurekubernetesrepo